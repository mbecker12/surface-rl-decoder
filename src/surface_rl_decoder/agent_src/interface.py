
import torch
from syndrome_masks import plaquette_mask, vertex_mask

class Interface(self, network)


    def interface(self, state)
    
        x = state*plaquette_mask
        z = state*vertex_mask


        return x, z, state